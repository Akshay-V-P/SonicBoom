
    <style>

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .account-btn {
            background-color: #333;
            color: #fff;
            border: none;
            padding: 8px 16px;
            border-radius: 6px;
            font-size: 14px;
            cursor: pointer;
        }

        .page-title {
            font-size: 24px;
            font-weight: 600;
        }

        /* Orders Section */
        .orders-section {
            background-color: #1a1a1a;
            border-radius: 8px;
            border: 1px solid #333;
            overflow: hidden;
            padding: 20px;
        }

        .orders-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 20px;
        }
        
        .orders-title {
            font-size: 18px;
            font-weight: 600;
        }

        .search-and-filter {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .search-input {
            width: 300px;
            padding: 8px 12px;
            border-radius: 4px;
            border: 1px solid #333;
            background-color: #2a2a2a;
            color: #fff;
            font-size: 14px;
        }
        .search-input:focus {
            outline: none;
            border-color: #4a90e2;
            box-shadow: 0 0 4px rgba(74, 144, 226, 0.5);
        }

        .search-btn {
            background-color: #4a90e2;
            color: #fff;
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
        }
        
        .filter-select {
            padding: 8px 12px;
            border-radius: 4px;
            border: 1px solid #333;
            background-color: #2a2a2a;
            color: #fff;
            font-size: 14px;
            cursor: pointer;
        }
        
        .orders-table-container {
            overflow-x: auto;
        }

        .orders-table {
            width: 100%;
            border-collapse: collapse;
        }

        .table-header th {
            padding: 15px 12px;
            text-align: left;
            font-size: 12px;
            color: #666;
            text-transform: uppercase;
            font-weight: 500;
            border-bottom: 1px solid #333;
            white-space: nowrap;
        }

        .table-header th:first-child {
            width: 50px;
            text-align: center;
        }

        .table-row {
            border-bottom: 1px solid #333;
        }

        .table-row:last-child {
            border-bottom: none;
        }

        .table-row td {
            padding: 15px 12px;
            font-size: 14px;
            vertical-align: middle;
            white-space: nowrap;
        }

        .checkbox-cell {
            text-align: center;
        }

        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
            text-align: center;
        }

        .status-paid {
            background-color: #34c759;
            color: #1a1a1a;
        }
        
        .status-unpaid {
            background-color: #ff3b30;
            color: #fff;
        }
        
        .status-cancelled {
            background-color: #6c757d;
            color: #fff;
        }
        
        .pagination {
            padding-top: 20px;
            border-top: 1px solid #333;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            gap: 10px;
            margin-top: 20px;
        }

        .pagination-info {
            color: #666;
            font-size: 14px;
        }

        .pagination-controls {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .pagination-btn {
            background-color: #333;
            color: #fff;
            border: none;
            width: 32px;
            height: 32px;
            border-radius: 4px;
            font-size: 13px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .pagination-btn:disabled {
            background-color: #1a1a1a;
            color: #666;
            cursor: not-allowed;
        }

        .pagination-btn.active {
            background-color: #4a90e2;
        }
        #clearBtn{
             background-color: #5f5f5f;
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            font-size: 14px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .my-swal-border {
            border: 1px solid #404044 !important;
            /* Change color and thickness */
            border-radius: 15px !important;
	    }
        
        @media (max-width: 768px) {
            .orders-header {
                flex-direction: column;
                align-items: stretch;
            }

            .search-and-filter {
                flex-direction: column;
            }

            .search-input, .search-btn, .filter-select {
                width: 100%;
            }
            
            .orders-table,
            .orders-table thead,
            .orders-table tbody,
            .orders-table th,
            .orders-table td,
            .orders-table tr {
                display: block;
            }
            
            .orders-table thead tr {
                position: absolute;
                top: -9999px;
                left: -9999px;
            }

            .orders-table tr {
                border: 1px solid #333;
                margin-bottom: 10px;
                padding: 10px;
                border-radius: 8px;
            }

            .orders-table td {
                border: none;
                position: relative;
                padding: 8px 0 8px 30%;
                text-align: right;
            }

            .orders-table td:before {
                content: attr(data-label) ": ";
                position: absolute;
                left: 10px;
                width: 28%;
                padding-right: 10px;
                white-space: nowrap;
                color: #666;
                font-size: 12px;
                text-transform: uppercase;
                text-align: left;
            }
        }
       
    </style>
        <input type="text" id="pageName" value="orders" hidden>
            <div class="header">
                <h1 class="page-title">Orders</h1>
                <button class="account-btn">Account</button>
            </div>

            <div class="orders-section">
                <div class="orders-header">
                    <input type="text" class="search-input" placeholder="Search">
                    <div class="search-and-filter">
                        <select name="amount" id="amount" class="filter-select">
                            <option value="">Amount</option>
                            <option value="1">Ascending</option>
                            <option value="-1">Descending</option>
                        </select>
                        <select name="payment" id="payment" class="filter-select">
                            <option value="">Payment</option>
                            <option value="UPI">UPI</option>
                            <option value="COD">COD</option>
                            <option value="bank">BANK</option>
                        </select>
                        <select name="status" id="status" class="filter-select">
                            <option value="">Payment Status</option>
                            <option value="paid">Paid</option>
                            <option value="unpaid">Unpaid</option>
                            <option value="failed">Cancelled</option>
                        </select>
                        <select name="status" id="order-status" class="filter-select">
                            <option value="">Status</option>
                            <option value="processing">Processing</option>
                            <option value="shipped">Shipped</option>
                            <option value="out-for-delivery">Out for delivery</option>
                            <option value="delivered">Delivered</option>
                            <option value="cancelled">Cancelled</option>
                        </select>
                        <button class="search-btn" id="search">Search</button>
                        <button id="clearBtn">Clear</button>
                    </div>
                </div>

                <div class="orders-table-container">
                    <table class="orders-table">
                        <thead class="table-header">
                            <tr>
                                <th>
                                    NO
                                </th>
                                <th>Reference No</th>
                                <th>Amount</th>
                                <th>Payment</th>
                                <th>Payment Status</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody id="tableBody">
                            <tr class="table-row">
                                <td class="checkbox-cell" data-label="All">1</td>
                                <td data-label="Reference No">#112-11321112344</td>
                                <td data-label="Amount">₹1249</td>
                                <td data-label="Payment">UPI</td>
                                <td data-label="Status"><span class="status-badge status-paid">Paid</span></td>
                                <td data-label="Status"><span class="status-badge status-paid">Delivered</span></td>
                            </tr>
                            <tr class="table-row">
                                <td class="checkbox-cell" data-label="All">2</td>
                                <td data-label="Reference No">#112-11321112344</td>
                                <td data-label="Amount">₹2999</td>
                                <td data-label="Payment">COD</td>
                                <td data-label="Status"><span class="status-badge status-unpaid">Unpaid</span></td>
                                <td data-label="Status"><span class="status-badge status-unpaid">Cancelled</span></td>
                            </tr>
                            <tr class="table-row">
                                <td class="checkbox-cell" data-label="All">3</td>
                                <td data-label="Reference No">#112-11321112344</td>
                                <td data-label="Amount">₹4563</td>
                                <td data-label="Payment">BANK</td>
                                <td data-label="Status"><span class="status-badge status-paid">Paid</span></td>
                                <td data-label="Status"><span class="status-badge status-paid">Delivered</span></td>
                            </tr>
                            <tr class="table-row">
                                
                                <td class="checkbox-cell" data-label="All">4</td>
                                <td data-label="Reference No">#112-11321112344</td>
                                <td data-label="Amount">₹1999</td>
                                <td data-label="Payment">UPI</td>
                                <td data-label="Status"><span class="status-badge status-paid">Paid</span></td>
                                <td data-label="Status"><span class="status-badge status-paid">Delivered</span></td>
                            </tr>
                            
                        </tbody>
                    </table>
                </div>

                <div class="pagination" id="pagination-container">
                    
                </div>
            </div>

<script>
    let current = 1
    let lim = 10
    document.addEventListener('DOMContentLoaded', function(){
        current = localStorage.getItem('currentPage')
        fetchOrders(`/admin/orders/search?currentPage=${current}&limit=10`)
    })

    document.getElementById('search').addEventListener('click', ()=>{
        current = localStorage.getItem('ordersCurrentPage')
        let url = `/admin/orders/search?currentPage=${current}`
        const search = document.querySelector('.search-input').value.trim()
        const filterAmount = document.getElementById('amount').value.trim()
        const filterPayment = document.getElementById('payment').value.trim()
        const filterStatus = document.getElementById('status').value.trim()
        const filterOrderStatus = document.getElementById('order-status').value.trim()
        if(search){
            url += `&limit=1&search=${search}`
        }else{
            url += `&limit=10`
        }
        if(filterAmount){
            url += `&filterAmount=${filterAmount}`
        }
        if(filterPayment){
            url += `&filterPayment=${filterPayment}`
        }
        if(filterStatus){
            url += `&filterStatus=${filterStatus}`
        }
        if(filterOrderStatus){
            url += `&filterOrderStatus=${filterOrderStatus}`
        }
        fetchOrders(url)
        
    })

    document.getElementById('clearBtn').addEventListener('click', ()=>{
        const search = document.querySelector('.search-input')
        const filterAmount = document.getElementById('amount')
        const filterPayment = document.getElementById('payment')
        const filterStatus = document.getElementById('status')
        search.value = ''
        filterAmount.value = ''
        filterPayment.value = ''
        filterStatus.value = ''
        fetchOrders('/admin/orders/search?currentPage=1&limit=10')
    })

    {{!-- document.querySelectorAll('.orderDetails').forEach((element)=>{
        console.log(element)
        element.addEventListener('click', ()=>{
            console.log('Get in')
            const value = element.querySelector('.userId').value
            console.log(value)
            window.location.href = `/admin/orders/details?orderId=${value}`
        })
    }) --}}

    function fetchOrders(url){
        fetch(url)
            .then(res => {
                if(!res.ok) throw new Error("Failed to fetch")
                return res.json()
            })
            .then(data => {
                const tbody = document.getElementById('tableBody')
                tbody.innerHTML = ""
                if(data.result.length == 0){
                    tbody.innerHTML = `<tr><td colspan="6" style="text-align:center;color:#999;">No orders found</td></tr>`
                    return
                }
                let index = 0
                data.result.forEach((order)=>{
                    tbody.innerHTML += `
                        <tr class="table-row orderDetails" data-orderid="${order.orderId}">
                            <td class="checkbox-cell" data-label="All">${++index}</td>
                            <td data-label="Reference No">${order.orderId}</td>
                            <td data-label="Amount">₹${order.total}</td>
                            <td data-label="Payment">${order.paymentMethod}</td>
                            <td data-label="Status"><span class="status-badge ${order.paymentStatus == 'paid'? 'status-paid': 'status-unpaid'}">${order.paymentStatus}</span></td>
                            <td data-label="Status"><span class="status-badge ${order.status.toLowerCase() == 'cancelled'? 'status-unpaid': 'status-paid'}">${order.status}</span></td>
                        </tr>`
                })

                document.querySelectorAll('.orderDetails').forEach((e)=>{
                    e.addEventListener('click', ()=>{
                        console.log('Get in')
                        const value = e.dataset.orderid
                        window.location.href = `/admin/orders/details?orderId=${value}`
                    })
                })

                current = data.currentPage
                localStorage.setItem('ordersCurrentPage', current)
                const pagination = document.getElementById('pagination-container')
                
                // setting url for next Page
                let tempUrl = new URL(url, window.location.origin);
                let currentPage = parseInt(tempUrl.searchParams.get("currentPage"), 10);
                tempUrl.searchParams.set("currentPage", currentPage+1);

                let nextUrl = tempUrl.pathname + '?' + tempUrl.searchParams.toString();
                
                // setting url for previous page
                tempUrl = new URL(url, window.location.origin);
                currentPage = parseInt(tempUrl.searchParams.get("currentPage"), 10);
                tempUrl.searchParams.set("currentPage", currentPage-1);

                let prevUrl = tempUrl.pathname + '?' + tempUrl.searchParams.toString();

                
                pagination.innerHTML = `
                    <div class="pagination-info">
                        ${data.currentPage} to ${data.totalPages} Pages
                    </div>
                    <div class="pagination-controls">
                        <button onclick="fetchOrders('${prevUrl}')" class="pagination-btn" style="${data.currentPage>1? "" : "display:none;"}">&lt;</button>
                        <button class="pagination-btn active">
                            ${data.currentPage}
                        </button>
                        <button onclick="fetchOrders('${nextUrl}')" class="pagination-btn" style="${data.currentPage<data.totalPages? "":"display:none;"}">&gt;</button>
                    </div>
                `
                

            })
            .catch(error => console.log(error))
        }
</script>
